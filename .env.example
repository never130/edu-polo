# Configuración de Django
DEBUG=0
SECRET_KEY=cambiar_esta_clave_por_una_segura_y_larga_en_produccion
ALLOWED_HOSTS=midominio.com,www.midominio.com,127.0.0.1

# Base de Datos (PostgreSQL)
# Formato: postgres://usuario:password@host:port/nombre_db
DATABASE_URL=postgres://mi_usuario:mi_password@localhost:5432/mi_db

# Configuración de Email (SMTP)
EMAIL_BACKEND=django.core.mail.backends.smtp.EmailBackend
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_USE_TLS=True
EMAIL_HOST_USER=tu_email@gmail.com
EMAIL_HOST_PASSWORD=tu_password_app

# Seguridad (Opcional)
CSRF_TRUSTED_ORIGINS=https://midominio.com,https://www.midominio.com
